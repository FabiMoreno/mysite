---
import Layout from "../../layouts/Layout.astro";
import ButtonPrimary from "../../components/ButtonPrimary.astro";
import { getLangFromUrl, useTranslations } from '../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<Layout title="Fabián Moreno | Error 404">

   
    <main class=" w-full h-screen bg-card-surface flex flex-col justify-center [&>p]:text-center items-center px-4 gap-4">
        <img class="max-w-[18rem]" src="https://res.cloudinary.com/dfh5otihs/image/upload/v1695233966/error_2x_rbt9em.webp" >
        <p class=" text-overline text-color-caption uppercase">
            Error 404
        </p>
        <div class="flex flex-col gap-2  [&>p]:text-center">    
            <p class=" text-title3 font-normal text-color-title">
                {t('title-404')}
            </p>
            <p class=" text-action font-normal text-color-subtitle sm:max-w-md">
                {t('description-404')}
            </p>
        </div>
        <ButtonPrimary
            href={`/${lang}/`}
            target="_self"
        >
        {t('button-404')}
        <i class='bx bx-left-arrow-alt bx-sm color-button-iconSecondary' slot="before"></i>
        </ButtonPrimary>
    </main>


</Layout>